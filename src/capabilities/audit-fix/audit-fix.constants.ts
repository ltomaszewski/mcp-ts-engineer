/** Per-step timeout constants for audit-fix sub-capabilities */

/** Max time for lint fix step (10 min) */
export const LINT_FIX_TIMEOUT_MS = 600_000

/** Max time for deps fix step (5 min) */
export const DEPS_FIX_TIMEOUT_MS = 300_000
