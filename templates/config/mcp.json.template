{
  "mcpServers": {
    "ts-engineer": {
      "type": "stdio",
      "command": "mcpmon",
      "args": ["node", "{{BIN_PATH}}"],
      "env": {
        "MCPMON_WATCH": "{{WATCH_PATH}}"
      }
    }
  }
}
